import { Routes } from '@angular/router';
import { AdminChatsComponent } from './components/admin-chats/admin-chats.component';

export const routes: Routes = [
  { path: 'admin-secret-chats', component: AdminChatsComponent }
];
